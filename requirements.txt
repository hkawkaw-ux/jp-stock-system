# --- コア ---
pandas>=2.0
numpy>=1.24

# --- データ取得 ---
yfinance>=0.2          # 実データ取得（無料・APIキー不要・約15分遅延。非公式・商用不可）
requests>=2.31         # 予備（他データ源を追加する場合用）

# --- 可視化 / UI ---
matplotlib>=3.7
streamlit>=1.30
plotly>=5.18          # 対話的チャート（任意）

# --- 設定管理 ---
PyYAML>=6.0
python-dotenv>=1.0

# --- テスト ---
pytest>=7.4
